version: 1
frontend:
  phases:
    build:
      commands:
        - cd web-ui
        - npm ci
        - npm run build
  artifacts:
    baseDirectory: web-ui/.next
    files:
      - '**/*'
  cache:
    paths:
      - web-ui/node_modules/**/*
