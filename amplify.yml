version: 1
frontend:
  phases:
    build:
      commands:
        - cd web-ui
        - npm install
        - npm run build
  artifacts:
    baseDirectory: web-ui/.next
    files:
      - 'web-ui/**/*'
  cache:
    paths:
      - web-ui/node_modules/**/*
